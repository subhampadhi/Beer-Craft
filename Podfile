project 'Beer Craft.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Beer Craft' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for Beer Craft
  pod 'Moya'
  pod 'Kingfisher'
  
  target 'Beer CraftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Beer CraftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
